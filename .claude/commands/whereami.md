# /whereami — Reorientation

Read `.claude/memory.md` and `.claude/STATUS.md` silently, then give a single focused paragraph covering:
- Which project and branch we are on
- What we were doing when we last left off
- Where exactly we stopped
- What the immediate next step is

Do not summarize the full STATUS.md. Do not list recent steps. Just orient — one paragraph, enough to decide whether to continue or ask a clarifying question before starting work.

If `.claude/memory.md` is blank or contains only the template, say so and run the first-run initialization flow from CLAUDE.md instead.
